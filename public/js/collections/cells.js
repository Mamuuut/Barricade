// Generated by CoffeeScript 1.3.3
(function() {

  define(['backbone', 'CellModel'], function(Backbone, CellModel) {
    var CellGrid;
    CellGrid = Backbone.Collection.extend({
      model: CellModel,
      selected: void 0,
      initialize: function() {
        return this.on('change:selected', this.onSelected, this);
      },
      onSelected: function(model, selected) {
        if (selected) {
          if (this.selected) {
            this.selected.set({
              selected: false
            });
          }
          return this.selected = model;
        }
      },
      setHoverable: function(color) {
        return this.each(function(cell) {
          var isHoverable;
          isHoverable = color === cell.get('pawn');
          return cell.set('hoverable', isHoverable);
        });
      }
    });
    return CellGrid;
  });

}).call(this);
